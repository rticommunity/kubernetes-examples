<?xml version="1.0"?>
<dds  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://community.rti.com/schema/6.0.0/rti_cloud_discovery_service.xsd">

        <!-- 
    This is an empty configuration, which assumes default values for all the
    settings. (E.g. transport: udpv4:7400.
    You can override some of the settings through the command line options.
    -->
    <cloud_discovery_service name="default">
        <annotation>
            <documentation><![CDATA[ 
                Empty configuration. Assumes default values.
                ]]>                
            </documentation>
        </annotation>
    </cloud_discovery_service>    

</dds>
